{
  "name": "first-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "publish": "tsc && vite build && git add . && git commit -m \"building game\" && git push"
  },
  "devDependencies": {
    "@types/node": "^22.9.1",
    "typescript": "~5.6.2",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "phaser": "^3.86.0"
  }
}
